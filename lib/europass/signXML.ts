/**
 * MOCK: Returns unsigned XML until client provides .p12 eSeal file.
 * Replace body of this function with real node-forge signing when ESEAL_P12_PATH is set.
 */
export function signXML(xml: string): string {
  return xml;
}
